{
  "name": "hackspain-prosper-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts",
    "lab": "bun --cwd web dev --port 3000",
    "eval:text": "bun run src/eval-text.ts",
    "eval:scripted": "bun run src/eval/scripted.ts",
    "test": "bun test src"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^4.0.86",
    "ai": "^7.0.106",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2"
  }
}
